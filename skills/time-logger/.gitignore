# Personal data — never commit raw source data or generated time entries
raw/
time_logs/

# Machine-specific config generated by setup (gitignored so it stays local)
capabilities.yml
user-preferences.md

# Claude Code local settings (contains MCP UUIDs and session-specific permissions)
.claude/settings.local.json

# Python
__pycache__/
*.pyc

# macOS
.DS_Store
